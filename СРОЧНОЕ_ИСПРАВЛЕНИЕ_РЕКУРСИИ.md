# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ РЕКУРСИИ

## ❌ Проблема:
```
infinite recursion detected in policy for relation "profiles"
Failed to load resource: 500 Internal Server Error
```

Все запросы к базе падают с ошибкой 500, кнопки админки и модерации пропали.

---

## 🎯 Причина:

RLS политики создали **бесконечную рекурсию**:
- Чтобы проверить доступ к `profiles`, политика проверяет `is_admin` в `profiles`
- Чтобы проверить `is_admin`, нужно снова обратиться к `profiles`
- И так до бесконечности → PostgreSQL падает с ошибкой 500

---

## ⚡ РЕШЕНИЕ (5 минут):

### ШАГ 1: Выполни SQL в Supabase (2 минуты)

1. Открой https://supabase.com/dashboard
2. Выбери проект → **SQL Editor**
3. Открой файл `supabase/FIX_RECURSION.sql`
4. Скопируй весь код и выполни (Run)

**Что делает этот скрипт:**
- ✅ Удаляет все рекурсивные политики
- ✅ Создает простые политики БЕЗ рекурсии
- ✅ Добавляет Foreign Key для comments
- ✅ Создает индексы

---

### ШАГ 2: Обнови схему API (30 секунд)

1. **Settings** → **API**
2. Нажми **"Reload PostgREST Schema"** или **"Restart API"**
3. Подожди 10-15 секунд

---

### ШАГ 3: Перезайди на сайт (1 минута)

**ОБЯЗАТЕЛЬНО:**
1. Выйди из аккаунта на сайте
2. Войди заново

**Зачем:** Нужно обновить JWT токен сессии.

---

### ШАГ 4: Деплой кода (2 минуты)

Код тоже исправлен (убрана ошибка React):

```bash
git add .
git commit -m "fix: исправлена рекурсия RLS и ошибка React"
git push
```

---

## ✅ Проверка:

После выполнения всех шагов:

1. **Консоль браузера (F12):**
   - ❌ Не должно быть ошибок 500
   - ❌ Не должно быть "infinite recursion"
   - ❌ Не должно быть "Cannot update component"

2. **Главная страница:**
   - ✅ Кнопки "Админ панель" и "Модерация" должны появиться

3. **Админ панель (/admin):**
   - ✅ Статистика должна загрузиться

4. **Модерация (/moderation):**
   - ✅ Комментарии должны загрузиться

---

## 🔧 Что было исправлено:

### База данных:
**БЫЛО (рекурсия):**
```sql
CREATE POLICY "Admins can view all profiles"
ON public.profiles FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM public.profiles  -- ← РЕКУРСИЯ!
    WHERE id = auth.uid() AND is_admin = true
  )
  OR id = auth.uid()
);
```

**СТАЛО (без рекурсии):**
```sql
CREATE POLICY "Anyone can view profiles"
ON public.profiles FOR SELECT
TO authenticated
USING (true);  -- ← Просто и безопасно
```

### Код (ModerationPanel.tsx):
**БЫЛО (ошибка React):**
```typescript
if (!user || (!isAdmin && !isModerator)) {
  navigate('/');  // ← setState во время рендера!
  return null;
}
```

**СТАЛО (правильно):**
```typescript
if (!user || (!isAdmin && !isModerator)) {
  return <Navigate to="/" replace />;  // ← Компонент вместо функции
}
```

---

## 🤔 Почему это безопасно?

**Вопрос:** Если все могут видеть все профили, это не опасно?

**Ответ:** Нет, потому что:
1. Профили содержат только публичную информацию (username, avatar, bio)
2. Чувствительные данные (email, пароли) хранятся в `auth.users`, к которой нет доступа
3. Проверка прав админа делается на уровне приложения (фронтенд)
4. Критические операции (удаление, изменение) защищены другими политиками

---

## 📋 Краткая версия:

```bash
# 1. Выполни SQL: supabase/FIX_RECURSION.sql
# 2. Settings → API → Reload Schema
# 3. Выйди и войди на сайте
# 4. git add . && git commit -m "fix" && git push
```

---

## 🆘 Если не помогло:

### Ошибка 500 всё еще есть?
1. Проверь что SQL выполнился без ошибок
2. Убедись что API перезапущен (Reload Schema)
3. Подожди 1 минуту и обнови страницу

### Кнопки не появились?
1. Проверь что ты вышел и вошел заново
2. Очисти кэш браузера (Ctrl+Shift+Delete)
3. Проверь что `is_admin = true` в базе:
```sql
SELECT email, is_admin FROM profiles WHERE email = 'твой@email.com';
```

### Комментарии не загружаются?
1. Проверь что Foreign Key создан:
```sql
SELECT constraint_name FROM information_schema.table_constraints 
WHERE table_name = 'comments' AND constraint_name = 'comments_user_id_fkey';
```

---

## 📚 Файлы:

- `supabase/FIX_RECURSION.sql` - **ИСПОЛЬЗУЙ ЭТОТ!**
- `supabase/FIX_SIMPLE.sql` - старая версия (не используй)
- `supabase/FIX_RELATIONSHIPS_AND_POLICIES.sql` - старая версия (не используй)

---

## ⚠️ ВАЖНО:

**НЕ ЗАБУДЬ:**
1. ✅ Выполнить SQL
2. ✅ Перезапустить API
3. ✅ Выйти и войти на сайте
4. ✅ Задеплоить код

Без этих 4 шагов ничего не заработает!
