# 🚀 Deployment Guide - ShiKaraKa Anime Portal

Это подробное руководство по развертыванию ShiKaraKa на production с использованием бесплатных сервисов Supabase и Vercel.

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Настройка Supabase](#настройка-supabase)
3. [Подготовка GitHub репозитория](#подготовка-github-репозитория)
4. [Deployment на Vercel](#deployment-на-vercel)
5. [Post-Deployment проверка](#post-deployment-проверка)
6. [Troubleshooting](#troubleshooting)
7. [Мониторинг и обслуживание](#мониторинг-и-обслуживание)

---

## Предварительные требования

Перед началом убедитесь, что у вас есть:

- ✅ **GitHub аккаунт** - для хостинга кода
- ✅ **Vercel аккаунт** - для deployment (можно войти через GitHub)
- ✅ **Supabase аккаунт** - для backend сервисов
- ✅ **Node.js 18+** - для локальной разработки и тестирования
- ✅ **Git** - для version control

> 💡 **Все сервисы предоставляют бесплатные tier'ы, достаточные для production использования!**

---

## Настройка Supabase

Supabase предоставляет backend инфраструктуру: аутентификацию и PostgreSQL базу данных.

### Шаг 1: Создание проекта

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите **"Start your project"** или **"New Project"**
3. Войдите через GitHub (рекомендуется) или создайте аккаунт
4. Создайте новую организацию (если это первый проект)
5. Заполните данные проекта:
   - **Name**: `shikaraka-anime-portal` (или любое другое имя)
   - **Database Password**: Сгенерируйте надежный пароль (сохраните его!)
   - **Region**: Выберите ближайший к вашим пользователям регион
   - **Pricing Plan**: Free (достаточно для старта)
6. Нажмите **"Create new project"**
7. Дождитесь завершения настройки (обычно 1-2 минуты)

### Шаг 2: Настройка базы данных

После создания проекта нужно создать таблицы для приложения.

#### 2.1 Откройте SQL Editor

1. В левом меню выберите **"SQL Editor"**
2. Нажмите **"New query"**

#### 2.2 Создайте таблицу profiles

Скопируйте и выполните SQL из файла `supabase/001_create_profiles_table.sql`:

```sql
-- Create profiles table
CREATE TABLE profiles (
  id UUID REFERENCES auth.users(id) ON DELETE CASCADE PRIMARY KEY,
  username TEXT,
  avatar_url TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Enable Row Level Security
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

-- Policy: Users can view their own profile
CREATE POLICY "Users can view own profile"
  ON profiles FOR SELECT
  USING (auth.uid() = id);

-- Policy: Users can update their own profile
CREATE POLICY "Users can update own profile"
  ON profiles FOR UPDATE
  USING (auth.uid() = id);

-- Policy: Users can insert their own profile
CREATE POLICY "Users can insert own profile"
  ON profiles FOR INSERT
  WITH CHECK (auth.uid() = id);

-- Create function to handle new user creation
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, username)
  VALUES (NEW.id, NEW.email);
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Trigger to automatically create profile on user signup
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();
```

Нажмите **"Run"** (или Ctrl+Enter)

#### 2.3 Создайте таблицу favorites

Создайте новый query и выполните SQL из файла `supabase/002_create_favorites_table.sql`:

```sql
-- Create favorites table
CREATE TABLE favorites (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  shikimori_id INTEGER NOT NULL,
  anime_name TEXT NOT NULL,
  added_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(user_id, shikimori_id)
);

-- Enable Row Level Security
ALTER TABLE favorites ENABLE ROW LEVEL SECURITY;

-- Policy: Users can view their own favorites
CREATE POLICY "Users can view own favorites"
  ON favorites FOR SELECT
  USING (auth.uid() = user_id);

-- Policy: Users can insert their own favorites
CREATE POLICY "Users can insert own favorites"
  ON favorites FOR INSERT
  WITH CHECK (auth.uid() = user_id);

-- Policy: Users can delete their own favorites
CREATE POLICY "Users can delete own favorites"
  ON favorites FOR DELETE
  USING (auth.uid() = user_id);

-- Indexes for performance
CREATE INDEX idx_favorites_user_id ON favorites(user_id);
CREATE INDEX idx_favorites_shikimori_id ON favorites(shikimori_id);
CREATE INDEX idx_favorites_added_at ON favorites(added_at DESC);
```

Нажмите **"Run"**

#### 2.4 Проверьте таблицы

1. В левом меню выберите **"Table Editor"**
2. Вы должны увидеть две таблицы: `profiles` и `favorites`
3. Проверьте структуру каждой таблицы

### Шаг 3: Настройка аутентификации

#### 3.1 Настройте Email Provider

1. В левом меню выберите **"Authentication"** → **"Providers"**
2. Убедитесь, что **Email** provider включен (по умолчанию включен)
3. Настройте параметры (опционально):
   - **Enable email confirmations**: Отключите для упрощения (или оставьте включенным для production)
   - **Secure email change**: Рекомендуется оставить включенным

#### 3.2 Настройте Site URL (важно!)

1. Перейдите в **"Authentication"** → **"URL Configuration"**
2. Установите **Site URL**:
   - Для локальной разработки: `http://localhost:5173`
   - После deployment на Vercel: `https://your-app.vercel.app`
3. Добавьте **Redirect URLs**:
   - `http://localhost:5173/**` (для разработки)
   - `https://your-app.vercel.app/**` (после deployment)

> ⚠️ **Важно**: После deployment на Vercel вернитесь сюда и обновите URLs!

### Шаг 4: Получите API credentials

1. В левом меню выберите **"Settings"** → **"API"**
2. Скопируйте следующие значения:

   **Project URL**:
   ```
   https://xxxxxxxxxxxxx.supabase.co
   ```

   **anon/public key** (в разделе "Project API keys"):
   ```
   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

3. Сохраните эти значения - они понадобятся для настройки приложения

> 🔒 **Безопасность**: `anon` ключ безопасен для использования в клиентском коде. Row Level Security защищает данные.

### Шаг 5: Настройте локальное окружение

1. Создайте файл `.env` в корне проекта:
```bash
cp .env.example .env
```

2. Откройте `.env` и вставьте ваши Supabase credentials:
```env
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

3. Проверьте подключение:
```bash
npm run dev
```

Откройте http://localhost:5173 и попробуйте зарегистрироваться.

---

## Подготовка GitHub репозитория

### Шаг 1: Инициализируйте Git (если еще не сделано)

```bash
git init
```

### Шаг 2: Проверьте .gitignore

Убедитесь, что `.gitignore` содержит:

```gitignore
# Dependencies
node_modules/

# Build output
dist/
build/

# Environment variables
.env
.env.local
.env.production.local
.env.development.local

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Testing
coverage/
```

> ⚠️ **Критически важно**: Никогда не коммитьте `.env` файл с реальными credentials!

### Шаг 3: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите **"New repository"**
3. Заполните данные:
   - **Repository name**: `shikaraka-anime-portal`
   - **Description**: "Modern anime portal with Shikimori API integration"
   - **Visibility**: Public или Private (на ваш выбор)
   - **НЕ** инициализируйте с README, .gitignore или license (они уже есть)
4. Нажмите **"Create repository"**

### Шаг 4: Запушьте код

```bash
# Добавьте все файлы
git add .

# Создайте первый commit
git commit -m "Initial commit: ShiKaraKa anime portal"

# Добавьте remote
git remote add origin https://github.com/yourusername/shikaraka-anime-portal.git

# Запушьте код
git push -u origin main
```

> 💡 Если ваша default ветка называется `master`, используйте `git push -u origin master`

---

## Deployment на Vercel

Vercel предоставляет бесплатный хостинг для React приложений с автоматическим CI/CD.

### Шаг 1: Создайте аккаунт на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"** (рекомендуется)
4. Авторизуйте Vercel для доступа к вашим репозиториям

### Шаг 2: Импортируйте проект

1. На dashboard Vercel нажмите **"Add New..."** → **"Project"**
2. Найдите репозиторий `shikaraka-anime-portal` в списке
3. Нажмите **"Import"**

### Шаг 3: Настройте проект

На странице конфигурации:

#### 3.1 Framework Preset
- Vercel автоматически определит **Vite**
- Если нет, выберите **Vite** из выпадающего списка

#### 3.2 Root Directory
- Оставьте пустым (проект в корне репозитория)

#### 3.3 Build and Output Settings
Vercel автоматически настроит:
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

Если нужно изменить, нажмите **"Override"**

#### 3.4 Environment Variables (ВАЖНО!)

Нажмите **"Environment Variables"** и добавьте:

| Name | Value |
|------|-------|
| `VITE_SUPABASE_URL` | `https://xxxxxxxxxxxxx.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` |

> 💡 Используйте те же значения, что и в локальном `.env` файле

Для каждой переменной:
1. Введите **Name**
2. Введите **Value**
3. Выберите **All** (Production, Preview, Development)
4. Нажмите **"Add"**

### Шаг 4: Deploy!

1. Нажмите **"Deploy"**
2. Vercel начнет сборку проекта (обычно 1-3 минуты)
3. Следите за логами сборки в реальном времени

### Шаг 5: Получите production URL

После успешного deployment:

1. Vercel покажет **"Congratulations!"** экран
2. Скопируйте ваш production URL: `https://your-app.vercel.app`
3. Нажмите **"Visit"** чтобы открыть приложение

### Шаг 6: Настройте custom domain (опционально)

1. В настройках проекта выберите **"Domains"**
2. Нажмите **"Add"**
3. Введите ваш домен (например, `shikaraka.com`)
4. Следуйте инструкциям для настройки DNS

---

## Post-Deployment проверка

После deployment обязательно проверьте все функции.

### Шаг 1: Обновите Supabase Site URL

> ⚠️ **Критически важно для работы аутентификации!**

1. Вернитесь в Supabase dashboard
2. Перейдите в **"Authentication"** → **"URL Configuration"**
3. Обновите **Site URL** на ваш Vercel URL:
   ```
   https://your-app.vercel.app
   ```
4. Добавьте в **Redirect URLs**:
   ```
   https://your-app.vercel.app/**
   ```
5. Нажмите **"Save"**

### Шаг 2: Тестирование функциональности

Откройте ваше приложение и проверьте:

#### ✅ Главная страница
- [ ] Загружаются популярные аниме
- [ ] Отображаются карточки с постерами, названиями, рейтингами
- [ ] Skeleton screens показываются при загрузке
- [ ] Адаптивная сетка работает (проверьте на мобильном)

#### ✅ Поиск
- [ ] Поле поиска в Header работает
- [ ] Результаты поиска отображаются корректно
- [ ] "Ничего не найдено" показывается для пустых результатов
- [ ] Очистка поиска возвращает популярные аниме

#### ✅ Аутентификация
- [ ] Форма регистрации работает
- [ ] Можно создать новый аккаунт
- [ ] Форма входа работает
- [ ] После входа отображается email пользователя в Header
- [ ] Выход из системы работает

> 💡 Если email confirmation включен в Supabase, проверьте почту для подтверждения

#### ✅ Страница аниме
- [ ] Клик на карточку открывает страницу деталей
- [ ] Отображается постер, жанры, статус, студия
- [ ] Видеоплеер загружается
- [ ] Кнопка "Добавить в список" видна для авторизованных пользователей
- [ ] Адаптивный layout работает на мобильных

#### ✅ Избранное
- [ ] Можно добавить аниме в избранное
- [ ] Иконка сердца меняется на заполненную
- [ ] Страница /favorites показывает избранные аниме
- [ ] Можно удалить аниме из избранного

#### ✅ Производительность
- [ ] Страницы загружаются быстро (< 3 секунд)
- [ ] Нет ошибок в консоли браузера
- [ ] Изображения загружаются с lazy loading
- [ ] Навигация между страницами плавная

### Шаг 3: Проверьте мобильную версию

Откройте приложение на мобильном устройстве или используйте DevTools:

1. Откройте Chrome DevTools (F12)
2. Нажмите Toggle Device Toolbar (Ctrl+Shift+M)
3. Выберите различные устройства:
   - iPhone SE (375px)
   - iPhone 12 Pro (390px)
   - iPad (768px)
   - Desktop (1920px)

Проверьте:
- [ ] Сетка адаптируется (2/4/6 колонок)
- [ ] Header адаптивный
- [ ] Кнопки достаточно большие для touch (44x44px)
- [ ] Текст читаемый
- [ ] Видеоплеер сохраняет соотношение 16:9

### Шаг 4: Мониторинг ошибок

1. В Vercel dashboard откройте ваш проект
2. Перейдите в **"Logs"** → **"Runtime Logs"**
3. Проверьте отсутствие ошибок

В Supabase:
1. Перейдите в **"Logs"** → **"API Logs"**
2. Проверьте успешные запросы к базе данных

---

## Troubleshooting

### Проблема: Приложение не загружается (белый экран)

**Решение**:
1. Откройте консоль браузера (F12)
2. Проверьте ошибки JavaScript
3. Убедитесь, что environment variables установлены в Vercel
4. Проверьте логи сборки в Vercel dashboard

### Проблема: Аутентификация не работает

**Возможные причины**:

1. **Site URL не настроен в Supabase**
   - Перейдите в Supabase → Authentication → URL Configuration
   - Установите Site URL на ваш Vercel URL
   - Добавьте Redirect URLs

2. **Environment variables не установлены**
   - Проверьте Vercel → Settings → Environment Variables
   - Убедитесь, что `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` установлены
   - После изменения переменных нужен redeploy!

3. **Email confirmation включен**
   - Проверьте почту для подтверждения email
   - Или отключите в Supabase → Authentication → Providers → Email

### Проблема: Данные аниме не загружаются

**Решение**:
1. Проверьте консоль на ошибки сети
2. Убедитесь, что Shikimori API доступен: https://shikimori.one/api/animes
3. Проверьте, что нет CORS ошибок
4. Проверьте React Query DevTools (если установлены)

### Проблема: Избранное не сохраняется

**Решение**:
1. Убедитесь, что пользователь авторизован
2. Проверьте Supabase → Table Editor → favorites
3. Проверьте Row Level Security policies:
   ```sql
   SELECT * FROM favorites; -- должно работать для авторизованного пользователя
   ```
4. Проверьте консоль на ошибки Supabase

### Проблема: 404 при обновлении страницы

**Причина**: SPA routing не настроен на сервере

**Решение**: Создайте `vercel.json` в корне проекта:

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

Закоммитьте и запушьте:
```bash
git add vercel.json
git commit -m "Add Vercel rewrites for SPA routing"
git push
```

Vercel автоматически задеплоит изменения.

### Проблема: Медленная загрузка

**Решения**:

1. **Проверьте размер bundle**:
   ```bash
   npm run build
   ```
   Проверьте размеры файлов в `dist/assets/`

2. **Оптимизируйте изображения**:
   - Используйте WebP формат
   - Добавьте lazy loading (уже реализовано)

3. **Проверьте React Query кэширование**:
   - Убедитесь, что `staleTime` установлен
   - Проверьте, что данные кэшируются

4. **Используйте Vercel Analytics**:
   - Включите в Vercel dashboard → Analytics
   - Анализируйте Core Web Vitals

---

## Мониторинг и обслуживание

### Vercel Analytics

1. В Vercel dashboard откройте проект
2. Перейдите в **"Analytics"**
3. Включите **"Web Analytics"** (бесплатно)
4. Мониторьте:
   - Page views
   - Unique visitors
   - Top pages
   - Devices и browsers

### Supabase Monitoring

1. В Supabase dashboard перейдите в **"Reports"**
2. Мониторьте:
   - API requests
   - Database size
   - Active users
   - Auth events

### Автоматический Deployment

Vercel автоматически деплоит при каждом push в GitHub:

- **Production**: Push в `main` ветку → deployment на production URL
- **Preview**: Push в другие ветки → preview deployment с уникальным URL
- **Pull Requests**: Автоматические preview deployments для PR

### Обновление зависимостей

Регулярно обновляйте зависимости:

```bash
# Проверьте устаревшие пакеты
npm outdated

# Обновите все minor/patch версии
npm update

# Обновите major версии (осторожно!)
npm install react@latest react-dom@latest
```

После обновления:
1. Запустите тесты: `npm test`
2. Проверьте локально: `npm run dev`
3. Закоммитьте и запушьте

### Backup базы данных

Supabase автоматически создает backups, но можно сделать вручную:

1. В Supabase dashboard перейдите в **"Database"** → **"Backups"**
2. Нажмите **"Create backup"**
3. Backups хранятся 7 дней на Free tier

### Мониторинг ошибок (опционально)

Для production рекомендуется интегрировать error tracking:

**Sentry** (бесплатный tier):
```bash
npm install @sentry/react
```

Настройте в `main.tsx`:
```typescript
import * as Sentry from "@sentry/react";

Sentry.init({
  dsn: "your-sentry-dsn",
  environment: import.meta.env.MODE,
});
```

---

## Полезные ссылки

### Документация
- [Supabase Docs](https://supabase.com/docs)
- [Vercel Docs](https://vercel.com/docs)
- [Vite Docs](https://vitejs.dev/)
- [React Query Docs](https://tanstack.com/query/latest)

### Dashboards
- [Supabase Dashboard](https://app.supabase.com)
- [Vercel Dashboard](https://vercel.com/dashboard)
- [GitHub Repository](https://github.com/yourusername/shikaraka-anime-portal)

### Support
- [Supabase Discord](https://discord.supabase.com)
- [Vercel Discord](https://discord.gg/vercel)

---

## Чеклист финального deployment

Перед тем как считать deployment завершенным, убедитесь:

- [ ] Supabase проект создан и настроен
- [ ] SQL миграции выполнены (profiles, favorites)
- [ ] Row Level Security policies работают
- [ ] Supabase Site URL обновлен на production URL
- [ ] GitHub репозиторий создан и код запушен
- [ ] `.env` файл НЕ закоммичен
- [ ] Vercel проект создан и настроен
- [ ] Environment variables установлены в Vercel
- [ ] Production deployment успешен
- [ ] Все функции протестированы на production
- [ ] Мобильная версия работает корректно
- [ ] Нет ошибок в консоли и логах
- [ ] Аутентификация работает
- [ ] Избранное сохраняется и загружается
- [ ] Поиск работает
- [ ] Видеоплеер загружается
- [ ] Performance приемлемый (< 3 сек загрузка)

---

**Поздравляем! 🎉 Ваше приложение ShiKaraKa теперь в production!**

Если возникли вопросы или проблемы, обратитесь к разделу [Troubleshooting](#troubleshooting) или откройте Issue в GitHub.
