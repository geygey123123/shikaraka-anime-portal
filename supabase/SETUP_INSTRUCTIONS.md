# 🚀 Инструкция по настройке базы данных ShiKaraKa

## Шаг 1: Применить SQL миграции

1. Откройте **Supabase Dashboard**: https://supabase.com/dashboard
2. Выберите ваш проект **ShiKaraKa**
3. Перейдите в раздел **SQL Editor** (слева в меню)
4. Нажмите **New Query**
5. Скопируйте **весь** код из файла `supabase/APPLY_ALL_MIGRATIONS.sql`
6. Вставьте в редактор SQL
7. Нажмите **Run** (или Ctrl+Enter)

✅ Вы должны увидеть сообщение об успешном выполнении и список созданных таблиц.

---

## Шаг 2: Создать Storage Bucket для аватаров

### 2.1 Создание bucket

1. В Supabase Dashboard перейдите в раздел **Storage** (слева в меню)
2. Нажмите **Create a new bucket**
3. Заполните форму:
   - **Name**: `avatars`
   - **Public bucket**: ✅ **Включите** (поставьте галочку)
   - **File size limit**: `2 MB`
   - **Allowed MIME types**: `image/jpeg, image/png, image/webp`
4. Нажмите **Create bucket**

### 2.2 Настройка RLS политик для bucket

1. В списке buckets найдите `avatars`
2. Нажмите на три точки (...) справа от bucket
3. Выберите **Policies**
4. Нажмите **New Policy**

#### Политика 1: Публичное чтение (SELECT)
- **Policy name**: `Public avatars read`
- **Allowed operation**: `SELECT`
- **Target roles**: `public`
- **Policy definition**: 
  ```sql
  true
  ```
- Нажмите **Review** → **Save policy**

#### Политика 2: Загрузка своих аватаров (INSERT)
- **Policy name**: `Users can upload own avatar`
- **Allowed operation**: `INSERT`
- **Target roles**: `authenticated`
- **Policy definition**:
  ```sql
  (bucket_id = 'avatars'::text) AND ((storage.foldername(name))[1] = (auth.uid())::text)
  ```
- Нажмите **Review** → **Save policy**

#### Политика 3: Обновление своих аватаров (UPDATE)
- **Policy name**: `Users can update own avatar`
- **Allowed operation**: `UPDATE`
- **Target roles**: `authenticated`
- **Policy definition**:
  ```sql
  (bucket_id = 'avatars'::text) AND ((storage.foldername(name))[1] = (auth.uid())::text)
  ```
- Нажмите **Review** → **Save policy**

#### Политика 4: Удаление своих аватаров (DELETE)
- **Policy name**: `Users can delete own avatar`
- **Allowed operation**: `DELETE`
- **Target roles**: `authenticated`
- **Policy definition**:
  ```sql
  (bucket_id = 'avatars'::text) AND ((storage.foldername(name))[1] = (auth.uid())::text)
  ```
- Нажмите **Review** → **Save policy**

---

## Шаг 3: Проверка настройки

### 3.1 Проверка таблиц

1. Перейдите в **Table Editor**
2. Убедитесь, что созданы таблицы:
   - ✅ `profiles` (с полями: `id`, `username`, `avatar_url`, `bio`, `is_admin`, `last_active`)
   - ✅ `favorites` (с полями: `watch_status`, `status_updated_at`)
   - ✅ `moderators`
   - ✅ `comments`
   - ✅ `ratings`
   - ✅ `rate_limits`

### 3.2 Проверка Storage

1. Перейдите в **Storage**
2. Убедитесь, что bucket `avatars` создан и публичный
3. Проверьте, что настроены 4 RLS политики

### 3.3 Проверка через приложение

1. Запустите приложение: `npm run dev`
2. Зарегистрируйтесь или войдите
3. Перейдите в **Профиль** (через меню пользователя)
4. Попробуйте:
   - ✅ Изменить username
   - ✅ Добавить биографию
   - ✅ Загрузить аватар
   - ✅ Посмотреть статистику

---

## 🎯 Готово!

Теперь все функции профиля должны работать:
- ✅ Просмотр профиля
- ✅ Редактирование username и bio
- ✅ Загрузка аватара
- ✅ Статистика (избранное, комментарии, рейтинги)

---

## ⚠️ Возможные проблемы

### Ошибка: "relation profiles does not exist"
**Решение**: Убедитесь, что вы применили SQL миграции (Шаг 1)

### Ошибка при загрузке аватара: "Bucket not found"
**Решение**: Создайте bucket `avatars` (Шаг 2.1)

### Ошибка: "new row violates row-level security policy"
**Решение**: Проверьте RLS политики для bucket (Шаг 2.2)

### Статистика не отображается
**Решение**: Убедитесь, что таблицы `comments` и `ratings` созданы (Шаг 1)

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте логи Supabase в разделе **Logs**
3. Убедитесь, что все миграции применены успешно
